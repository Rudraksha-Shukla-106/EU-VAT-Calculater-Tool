{
  "name": "vatsnap",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "VatSnap — free EU VAT rate calculator for small online sellers. Static Astro site.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:cf": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "deploy:pages": "astro build && wrangler pages deploy dist --project-name=vatsnap",
    "astro": "astro",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.13.0",
    "eu-vat-rates-data": "^2026.8.23"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^4.131.1"
  }
}
